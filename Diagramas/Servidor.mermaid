sequenceDiagram
    participant Client as Cliente
    participant WebService as Servidor - Webservice
    participant DAO as Server DAO User

    Client->>WebService: Petición HTTP GET /users/&lt;username&gt;
    WebService->>DAO: getUserByUsername(username)
    DAO-->>WebService: Usuario (si existe)
    WebService-->>Client: JSON con datos de usuario
